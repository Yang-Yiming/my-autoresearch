# my-autoresearch

This directory is a reusable autoresearch harness embedded inside a host
project.

Read `config/program.md` for the generic workflow and `config/project.md` for
the current host project's objective, commands, metrics, and protected files.

Do not treat this directory as the whole project unless
`config/autoresearch.config.json` explicitly sets `workspace_dir` to `.`.
